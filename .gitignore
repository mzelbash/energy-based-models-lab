# Node dependencies (dev only: build scripts and smoke tests)
node_modules/

# PDF files (book chapter and personal notes), do not publish
*.pdf

# Local verification output
scratch_out/

# Temporary scratch scripts (underscore prefix)
build/_*.mjs

# OS and editor cruft
.DS_Store
Thumbs.db
*.log
